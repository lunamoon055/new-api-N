# 测试接口优化说明 - 显示真实的上游 API 调用信息

## 优化目标

在模型测试页面中展示真实的上游 API 接口调用详情，包括：
- 上游 API 的真实 URL
- 发送给上游的请求体（转换后的 payload）
- 请求头信息
- 上游返回的响应（预览）

这样可以帮助用户：
1. **调试问题**：看到实际发送给上游的请求格式
2. **学习转换规则**：了解客户端请求如何转换为上游格式
3. **验证配置**：确认 base_url、model_mapping 等配置是否生效
4. **对接参考**：作为直接调用上游 API 的参考示例

## 实现方案

### 后端修改

#### 1. 扩展 `testResult` 结构体（`controller/channel-test.go`）

```go
type testResult struct {
    context         *gin.Context
    localErr        error
    newAPIError     *types.NewAPIError
    upstreamURL     string              // 上游 API URL
    upstreamRequest json.RawMessage     // 上游请求体
    upstreamHeaders map[string]string   // 上游请求头
    upstreamResp    string              // 上游响应预览
}
```

#### 2. 收集调试信息（`testChannelWithPayload` 函数）

在发送请求前收集：
- 上游 URL：`channel.GetBaseURL() + c.Request.URL.Path`
- 请求头：收集 Content-Type、Accept 等关键头
- 请求体：转换后的 `jsonData`
- 渠道类型和模型名称

在接收响应后收集：
- 响应体预览：截取前 1000 字符

```go
// 收集上游请求信息用于调试
upstreamURL := channel.GetBaseURL() + c.Request.URL.Path
upstreamHeaders := make(map[string]string)
// 收集关键请求头（隐藏敏感信息）
for key := range c.Request.Header {
    if strings.Contains(strings.ToLower(key), "content") || 
       strings.Contains(strings.ToLower(key), "accept") {
        upstreamHeaders[key] = c.Request.Header.Get(key)
    }
}
upstreamHeaders["X-Request-Model"] = testModel
upstreamHeaders["X-Channel-Type"] = constant.GetChannelTypeName(channel.Type)

// ... 发送请求 ...

// 截取响应体用于调试展示（最多1000字符）
upstreamRespPreview := string(respBody)
if len(upstreamRespPreview) > 1000 {
    upstreamRespPreview = upstreamRespPreview[:1000] + "..."
}
```

#### 3. 返回调试信息（`TestChannel` 和 `TestChannelLab` 接口）

在成功响应中添加 `data` 字段：

```go
c.JSON(http.StatusOK, gin.H{
    "success": true,
    "message": "",
    "time":    consumedTime,
    "data": gin.H{
        "upstream_url":      result.upstreamURL,
        "upstream_request":  string(result.upstreamRequest),
        "upstream_headers":  result.upstreamHeaders,
        "upstream_response": result.upstreamResp,
    },
})
```

### 前端修改

#### 1. 更新类型定义（`web/default/src/features/channels/types.ts`）

```typescript
export interface ChannelTestResponse {
  success: boolean
  message?: string
  error_code?: string
  time?: number
  data?: {
    response_time?: number
    error?: string
    upstream_url?: string          // 新增
    upstream_request?: string      // 新增
    upstream_headers?: Record<string, string>  // 新增
    upstream_response?: string     // 新增
  }
}
```

#### 2. 显示调试信息（`channel-test-lab.tsx`）

在测试结果区域添加以下部分（仅在有数据时显示）：

1. **Upstream API URL**：显示完整的上游 API 地址
2. **Upstream Request Body**：显示发送给上游的请求体（JSON 格式）
3. **Upstream Headers**：显示请求头信息
4. **Upstream Response (Preview)**：显示上游返回的响应预览

每个部分都有"Copy"按钮方便复制。

```tsx
{result?.response?.data?.upstream_url && (
  <>
    <div className='grid gap-2'>
      <Label>{t('Upstream API URL')}</Label>
      <pre className='bg-muted/60 overflow-auto rounded-md border p-3 font-mono text-xs'>
        {result.response.data.upstream_url}
      </pre>
    </div>
    
    <div className='grid gap-2'>
      <Label>{t('Upstream Request Body')}</Label>
      <pre className='bg-muted/60 max-h-64 overflow-auto rounded-md border p-3 font-mono text-xs'>
        {result.response.data.upstream_request}
      </pre>
    </div>
    
    {/* ... 其他部分 ... */}
  </>
)}
```

## 使用效果

### 测试前

之前只能看到：
- 测试是否成功
- 响应时间
- 简单的错误信息

### 测试后（优化后）

现在可以看到：

#### 1. 上游 API URL
```
https://api.openai.com/v1/chat/completions
```

#### 2. 上游请求体
```json
{
  "model": "gpt-4o-mini",
  "messages": [
    {
      "role": "user",
      "content": "hi"
    }
  ],
  "max_tokens": 16,
  "stream": false
}
```

#### 3. 上游请求头
```json
{
  "Content-Type": "application/json",
  "Accept": "application/json",
  "X-Request-Model": "gpt-4o-mini",
  "X-Channel-Type": "OpenAI"
}
```

#### 4. 上游响应预览
```json
{
  "id": "chatcmpl-xxx",
  "object": "chat.completion",
  "created": 1234567890,
  "model": "gpt-4o-mini",
  "choices": [
    {
      "index": 0,
      "message": {
        "role": "assistant",
        "content": "Hello! How can I help you today?"
      }
    }
  ],
  "usage": {
    "prompt_tokens": 8,
    "completion_tokens": 9,
    "total_tokens": 17
  }
}
```

## 实际应用场景

### 场景 1：调试视频模型

测试视频模型时，可以看到：
- 实际发送的 prompt、size、duration 等参数
- 上游 API 的响应格式
- 任务 ID 和状态

例如测试 `sd2-0-fast`：

**上游 URL**:
```
https://sanbaobeauty.com/pg/video/async-generations
```

**上游请求**:
```json
{
  "model": "sd2_full",
  "prompt": "生成一个电影感产品短视频",
  "ratio": "9:16",
  "resolution": "720p",
  "duration": 5
}
```

### 场景 2：验证模型映射

当配置了 `model_mapping` 时，可以验证映射是否生效：

**客户端请求模型**: `gpt-4o-mini`  
**上游实际模型**: `gpt-4o-2024-08-06` （映射后）

在"Upstream Request Body"中可以看到实际发送的模型名称。

### 场景 3：学习 API 格式转换

对于不同的端点类型，可以看到请求格式的转换：

**Chat Completions** → OpenAI 格式
```json
{
  "model": "gpt-4o-mini",
  "messages": [...],
  "max_tokens": 16
}
```

**Anthropic Messages** → Claude 格式
```json
{
  "model": "claude-3-5-haiku-latest",
  "messages": [...],
  "max_tokens": 16
}
```

**Video Generation** → Task 格式
```json
{
  "model": "video-2.0-fast",
  "prompt": "a mountain at sunrise",
  "size": "720x1280",
  "duration": 4
}
```

### 场景 4：作为 API 调用参考

可以直接复制上游请求信息，用于：
- 编写客户端代码
- 测试 API 连通性
- 调试第三方集成
- 学习不同供应商的 API 格式

例如，复制后可以直接用 curl 测试：

```bash
curl https://api.openai.com/v1/chat/completions \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-4o-mini",
    "messages": [{"role": "user", "content": "hi"}],
    "max_tokens": 16
  }'
```

## 安全考虑

### 敏感信息保护

1. **API Key 隐藏**：不在请求头中显示 Authorization 头
2. **响应截断**：只显示前 1000 字符的响应预览
3. **仅测试场景**：只在测试接口中返回这些信息，正常代理请求不会暴露

### 访问控制

这些调试信息只在以下情况下可见：
- 用户有权限访问该渠道
- 在测试接口中显式调用
- 管理员和渠道所有者

## 总结

通过这次优化，测试功能变得更加透明和实用：

✅ **可见性提升**：清楚看到每个请求的完整流程  
✅ **调试效率**：快速定位问题所在（参数错误、格式不对等）  
✅ **学习价值**：了解不同 API 的请求格式和转换规则  
✅ **参考价值**：作为直接调用上游 API 的代码示例  

现在你可以在"模型测试"页面看到完整的上游 API 调用信息了！
