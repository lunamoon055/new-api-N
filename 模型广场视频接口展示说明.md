# 模型广场视频接口智能展示说明

## 功能概述

模型广场现在能够**智能识别视频模型**，并自动展示**真实的视频生成接口**，而不是伪装成 OpenAI Chat Completions 接口。

这个功能完全基于前端智能判断，**不会暴露任何上游信息**，只展示你自己的 API 网关地址和标准化的接口格式。

## 核心特性

### ✅ 智能识别

系统会自动识别以下类型的视频模型：

1. **异步视频模型**（使用 `/v1/video/async-generations`）：
   - sora、sora-2、sora2
   - video-2.0、video-2.5（包括 fast/mini 变体）
   - minimax-h3
   - kling、kling-v3
   - veo、veo31
   - ko3
   - wan3.0
   - grok-imagine-video

2. **同步视频模型**（使用 `/v1/videos`）：
   - sd2 系列（sd2-0-fast、sd2-full 等）
   - sd-2 系列（sd-2-c1、sd-2.5 等）
   - videos-* 系列
   - 004系列/*
   - omni-video-*
   - seedance2.0、seedance2.5
   - wan3.0-video
   - grok-imagine-video-1.5

3. **官转系列**（使用 `/v1/video/generations`）

4. **图像模型**（使用 `/v1/images/generations` 或异步接口）

### ✅ 真实接口展示

**视频模型示例**（以 sd2-0-fast 为例）：

```bash
curl -X POST "http://localhost:3000/v1/videos" \
  -H "Authorization: Bearer $NEW_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "sd2-0-fast",
    "prompt": "A cinematic shot of a cat walking through a sunlit garden.",
    "ratio": "16:9",
    "resolution": "720p",
    "duration": 5
  }'
```

**异步视频模型示例**（以 video-2.0-fast 为例）：

```bash
curl -X POST "http://localhost:3000/v1/video/async-generations" \
  -H "Authorization: Bearer $NEW_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "video-2.0-fast",
    "prompt": "A cinematic shot of a cat walking through a sunlit garden.",
    "size": "1280x720",
    "duration": 4
  }'
```

**对比：聊天模型仍然显示聊天接口**（以 gpt-4o 为例）：

```bash
curl http://localhost:3000/v1/chat/completions \
  -H "Authorization: Bearer $NEW_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-4o",
    "messages": [{"role": "user", "content": "Explain quantum entanglement"}]
  }'
```

## 实现原理

### 1. 模型识别逻辑

在 `model-endpoint-presets.ts` 文件中定义了识别规则：

```typescript
export function getModelEndpointPreset(modelName?: string): ModelEndpointPreset | null {
  const model = modelName?.trim().toLowerCase() ?? ''
  
  // 根据模型名称的前缀或模式进行匹配
  if (model.startsWith('sd2') || model.startsWith('sd-2-')) {
    return VIDEO_VIDEOS  // 返回 /v1/videos 端点
  }
  
  if (model.startsWith('sora') || model.startsWith('video-2.0')) {
    return VIDEO_ASYNC  // 返回 /v1/video/async-generations 端点
  }
  
  // ... 更多规则
}
```

### 2. 接口示例生成

在 `model-details-api.tsx` 中，`buildVideoSample` 函数会：

1. 检测模型类型（videos API 还是异步 API）
2. 根据类型选择合适的参数格式
3. 生成对应的 curl、Python、TypeScript 示例

```typescript
function buildVideoSample(lang: Lang, ctx: SampleContext): string {
  const isVideosApi = modelLower.startsWith('videos-') || 
                      modelLower.startsWith('sd2')
  
  const body: Record<string, any> = {
    model: ctx.modelName,
    prompt: 'A cinematic shot...',
  }
  
  // 根据 API 类型添加不同参数
  if (isVideosApi) {
    body.ratio = '16:9'
    body.resolution = '720p'
    body.duration = 5
  } else {
    body.size = '1280x720'
    body.duration = 4
  }
  
  // 生成示例代码
}
```

### 3. 端点信息展示

在模型详情页面会显示：

- **Request endpoint**：`POST /v1/videos`
- **Query endpoint**：`GET /v1/videos/{task_id}`（用于查询任务状态）

对于异步任务，还会显示轮询示例：

```bash
curl -X GET "http://localhost:3000/v1/videos/$TASK_ID" \
  -H "Authorization: Bearer $NEW_API_KEY"
```

## 安全性保证

### ❌ 不会暴露的信息

1. **上游 API 地址**：永远不显示真实的上游地址（如 `https://api.openai.com`）
2. **上游 API Key**：不显示渠道配置的真实密钥
3. **渠道信息**：不显示具体使用哪个渠道
4. **模型映射**：不显示模型名称映射关系
5. **内部配置**：不显示任何渠道的内部配置

### ✅ 只展示的信息

1. **你的网关地址**：`http://localhost:3000` 或用户的实际部署地址
2. **标准接口路径**：`/v1/videos`、`/v1/video/async-generations` 等
3. **请求参数格式**：标准的视频生成参数
4. **用户自己的 API Key 占位符**：`$NEW_API_KEY` 或 `<YOUR_API_KEY>`

## 用户体验

### 使用场景

1. **开发者参考**：
   - 查看模型详情时，立即看到正确的接口格式
   - 复制示例代码直接使用
   - 无需查阅文档就知道如何调用

2. **快速上手**：
   - 新用户不会被错误的接口格式误导
   - 明确知道视频模型使用视频接口，不是聊天接口
   - 参数示例清晰完整

3. **学习参考**：
   - 了解不同类型模型的调用方式
   - 看到真实可用的参数组合
   - 理解同步和异步接口的区别

### 页面展示

在模型广场点击视频模型（如 `sd2-0-fast`）后：

**API 标签页会显示**：

1. **Code samples**（代码示例）
   - 多语言切换：cURL / Python / TypeScript / JavaScript
   - 自动选择视频接口端点
   - 展示完整的请求示例

2. **Request endpoint**（请求端点）
   - 显示：`POST /v1/videos`
   - 显示完整 URL：`http://localhost:3000/v1/videos`

3. **Query endpoint**（查询端点，如果是异步任务）
   - 显示：`GET /v1/videos/{task_id}`
   - 提供查询任务状态的示例代码

4. **Authentication**（认证）
   - 说明如何使用 Bearer Token
   - 提示从哪里获取 API Key

5. **Supported parameters**（支持的参数）
   - 列出视频生成的参数
   - 显示参数类型、默认值、取值范围
   - 参数说明

## 支持的接口类型

| 接口类型 | 路径 | 用途 | 典型模型 |
|---------|------|------|---------|
| Chat Completions | `/v1/chat/completions` | 对话聊天 | gpt-4o, claude-3.5 |
| Responses | `/v1/responses` | OpenAI Responses | codex 系列 |
| Video (同步) | `/v1/videos` | 视频生成 | sd2-*, videos-* |
| Video (异步) | `/v1/video/async-generations` | 异步视频生成 | sora, video-2.0, kling |
| Video Generations | `/v1/video/generations` | 视频生成 | 官转系列 |
| Image Generations | `/v1/images/generations` | 图像生成 | dall-e, sd |
| Image (异步) | `/v1/images/async-generations` | 异步图像 | gpt-image2 |
| Embeddings | `/v1/embeddings` | 向量嵌入 | text-embedding-* |
| Anthropic Messages | `/v1/messages` | Claude 格式 | claude-* |
| Gemini | `/v1beta/models/{model}:generateContent` | Gemini 格式 | gemini-* |

## 常见问题

### Q1：为什么我的视频模型显示的是聊天接口？

**原因**：模型名称可能不在识别规则中。

**解决**：
1. 检查模型名称是否包含常见的视频模型前缀（sora、video、kling、sd2 等）
2. 如果是新的视频模型系列，可以在 `model-endpoint-presets.ts` 中添加识别规则
3. 或者在模型元数据中显式配置 `supported_endpoint_types`

### Q2：如何添加新的视频模型识别规则？

编辑 `web/default/src/features/pricing/lib/model-endpoint-presets.ts`：

```typescript
export function getModelEndpointPreset(modelName?: string): ModelEndpointPreset | null {
  const model = modelName?.trim().toLowerCase() ?? ''
  
  // 添加你的规则
  if (model.startsWith('your-video-prefix')) {
    return VIDEO_ASYNC  // 或 VIDEO_VIDEOS
  }
  
  // ... 现有规则
}
```

### Q3：接口示例中的参数是固定的吗？

不完全是。`buildVideoSample` 函数会根据模型类型智能选择参数：

- **videos API** 格式：`ratio`, `resolution`, `duration`
- **异步 API** 格式：`size`, `duration`

你可以根据实际需要调整这些默认参数。

### Q4：这个功能会影响实际的 API 调用吗？

**不会**。这个功能只影响模型广场的**展示**，不会改变实际的 API 路由或处理逻辑。

实际的 API 调用仍然由后端的路由和适配器处理，前端只是让用户看到正确的调用方式。

### Q5：如何验证视频模型是否被正确识别？

1. 在模型广场搜索你的视频模型
2. 点击查看模型详情
3. 查看 **API** 标签页
4. 检查 **Request endpoint** 是否显示视频接口（`/v1/videos` 或 `/v1/video/async-generations`）
5. 查看代码示例是否包含视频参数（`ratio`、`duration` 等）

## 维护指南

### 添加新的视频模型系列

1. 确定模型命名规则（如：新的 `video-3.0` 系列）
2. 确定使用哪个接口（同步 `/v1/videos` 还是异步）
3. 在 `getModelEndpointPreset` 函数中添加识别规则
4. 在 `buildVideoSample` 中调整参数（如果需要）

### 调整接口参数

如果某个视频 API 的参数格式变化：

1. 编辑 `buildVideoSample` 函数
2. 根据模型名称添加特殊处理
3. 更新参数示例

### 更新文档

如果添加了新的接口类型：

1. 在 `model-endpoint-presets.ts` 中定义新的 Preset
2. 在 `buildSample` 函数中添加对应的构建函数
3. 更新本文档的"支持的接口类型"表格

## 总结

通过智能识别模型类型，模型广场现在能够：

✅ **自动展示正确的接口格式**：视频模型显示视频接口，聊天模型显示聊天接口  
✅ **提供真实可用的示例代码**：参数完整、格式正确，可直接复制使用  
✅ **保护上游信息**：只展示你自己的网关地址，不暴露任何上游配置  
✅ **提升用户体验**：开发者无需查阅文档就能知道如何调用  
✅ **易于维护**：基于规则的识别逻辑，添加新模型很简单  

现在你的模型广场会智能地为每种模型类型展示最合适的 API 接口！🎉
