# 视频模型测试功能打通说明

## 问题描述

从截图中可以看到，当尝试测试视频模型（如 `gpt-image-2`）时，系统报错：

```
Request error occurred: 模型 gpt-image-2 价格未配置。请前往「系统设置 → 运营设置」开启自用模式，或在「系统设置 → 分组与模型定价设置」中为该模型配置价格。
```

这是因为视频模型在进行测试时，后端会检查模型是否配置了价格或倍率，如果没有配置就会报错。

## 已有功能

系统已经具备了完整的视频模型测试功能：

### 前端支持

- **测试端点类型**（`web/default/src/features/channels/lib/channel-test-lab.ts`）：
  - `openai-video`: 标准视频生成（/v1/videos）
  - `openai-video-async`: 异步视频生成（/v1/video/async-generations）
  - `sanbao-image`: 三宝图像生成
  - `sanbao-video`: 三宝视频生成
  - `sanbao-upload`: 三宝参考素材上传
  - `sanbao-image-poll`: 三宝图像任务轮询
  - `sanbao-video-poll`: 三宝视频任务轮询

### 后端支持

- **测试路由**（`controller/channel-test.go`）：
  - `TestChannel`: 基础测试接口（GET 参数）
  - `TestChannelLab`: 高级测试接口（支持自定义 payload）

- **视频模型识别**：
  - 自动识别视频模型（sora、kling、video-*、veo 等）
  - 支持同步和异步视频生成模式
  - 支持 videos API 格式（videos-*、sd2 等）

- **价格计算**：
  - 支持按次计费（ModelPriceHelperPerCall）
  - 支持按量计费（ModelPriceHelper）
  - 支持分辨率分级定价

## 修改内容

### 文件：`relay/helper/price.go`

修改了两个函数，让测试场景下即使没有配置价格也能通过：

#### 1. `ModelPriceHelperPerCall` 函数（第 206-213 行）

```go
// 在渠道测试场景下，如果没有配置价格/倍率，使用默认值以便测试通过
if info.IsChannelTest {
    if !ratioSuccess {
        modelRatio = 1.0
        ratioSuccess = true
    }
    acceptUnsetRatio = true
}
```

#### 2. `ModelPriceHelper` 函数（第 110-115 行）

```go
// 在渠道测试场景下，如果没有配置倍率，使用默认值以便测试通过
if info.IsChannelTest {
    modelRatio = 1.0
    success = true
    acceptUnsetRatio = true
}
```

## 修改原理

- **检测测试场景**：通过 `info.IsChannelTest` 标志判断是否为测试场景
- **提供默认值**：在测试场景下，如果模型没有配置价格或倍率，使用默认值 `1.0`
- **跳过价格检查**：设置 `acceptUnsetRatio = true`，允许未配置价格的模型通过测试

## 使用方法

### 1. 基础测试（模型测试页面）

在"渠道管理"页面，点击渠道的"测试"按钮：

- 系统会自动选择该渠道配置的测试模型
- 对于视频模型，会自动识别并使用对应的视频测试端点
- 即使模型没有配置价格，测试也能正常进行

### 2. 高级测试（API Test Lab）

在"模型测试"页面使用高级测试功能：

1. **选择渠道**：从下拉列表中选择要测试的渠道
2. **选择模型**：选择或输入视频模型名称
3. **选择端点类型**：
   - `Auto detect`: 自动检测（推荐）
   - `Video Generation`: /v1/videos（同步视频生成）
   - `Async Video Generation`: /v1/video/async-generations（异步视频生成）
   - 其他三宝相关端点
4. **自定义 payload**：可以编辑请求模板，自定义测试参数
5. **运行测试**：点击"Run Test"按钮

### 3. 支持的视频模型

系统自动识别以下视频模型：

- **OpenAI 系列**：sora、sora-2、sora2
- **Kling 系列**：kling、kling-v3
- **视频 2.0 系列**：video-2.0、video-2.0-fast、video-2.0-mini、video-2.5
- **MiniMax H3**：minimax-h3、minimax-h3-*
- **通义万相**：wan3.0、wan3.0-*
- **抖音系列**：004系列/*、omni-video-*
- **其他**：veo、veo31、ko3、grok-imagine-video、sd2 等

## 测试流程

1. **选择测试模型** → 系统自动识别模型类型
2. **确定测试端点** → 根据模型名称选择正确的 API 端点
3. **构建测试请求** → 生成符合端点格式的测试 payload
4. **价格检查** → **修改后：测试场景下使用默认价格，不会因为未配置价格而失败**
5. **发送请求** → 调用上游 API
6. **返回结果** → 显示测试结果和响应时间

## 注意事项

1. **测试不扣费**：测试过程中会记录消费日志，token_name 为"模型测试"，但不会真正扣除用户额度
2. **价格配置建议**：虽然测试时不需要配置价格，但在生产环境使用时仍需要为模型配置正确的价格
3. **自用模式**：如果开启了自用模式，所有模型都可以使用，不需要配置价格
4. **默认测试参数**：
   - 视频长度：4-5 秒
   - 分辨率：720p 或 1280x720
   - Prompt：简单的测试提示词（如"a mountain at sunrise"）

## 配置价格（可选）

如果需要在生产环境使用视频模型，建议配置价格：

1. 前往「系统设置 → 分组与模型定价设置」
2. 添加视频模型的价格配置：
   - **按次计费**：为每个模型设置固定价格（如 0.01 美元/次）
   - **按量计费**：设置模型倍率（如 1.5）
   - **分辨率分级**：为不同分辨率设置不同价格

或者：

1. 前往「系统设置 → 运营设置」
2. 开启「自用模式」：允许所有模型免费使用（不检查价格）

## 验证方法

修改完成后，测试以下场景：

1. ✅ 测试未配置价格的视频模型（如 gpt-image-2）
2. ✅ 测试已配置价格的视频模型
3. ✅ 使用不同的测试端点类型（auto、openai-video、openai-video-async）
4. ✅ 自定义 payload 进行测试
5. ✅ 测试通过后，查看消费日志是否正确记录

## 总结

通过此次修改：

- ✅ **打通了视频模型测试接口**：测试时不再因为价格未配置而失败
- ✅ **保持了价格检查机制**：生产环境仍会进行正常的价格检查
- ✅ **提升了测试体验**：可以快速验证渠道和模型的可用性
- ✅ **兼容性良好**：不影响其他模型类型的测试流程

现在，你可以在"模型测试"页面自由测试各种视频模型了！
